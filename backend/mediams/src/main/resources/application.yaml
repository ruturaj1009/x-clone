spring:
  application:
    name: mediaMS
  datasource:
    url: jdbc:mysql://localhost:3306/twitter_media
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none # or create, create-drop, none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect # or your specific dialect

cloudinary:
  cloud-name: ruturaj1009
  api-key: 624999354387888
  api-secret: --MRXrBerd2VpoGaztIDGcuzl1A

server:
  port: 8086 # or your desired port

logging:
  level:
    root: INFO
    org:
      springframework:
        web: INFO
    your:
      package: DEBUG # Replace 'your.package' with your application's package